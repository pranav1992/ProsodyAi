# Predictions — the three provided calls

`evaluation_batch_results.csv` / `.json` are the actual output of the
hosted dashboard (`http://13.127.143.24`, batch `bea26165-0d9e-4adf-b164-4c4818c724de`,
2026-09-01) for `call_001.ogg`, `call_002.ogg`, `call_003.ogg` — uploaded
through the live `/batches` API exactly as an evaluator would, not
generated by a local script. All 3 files processed successfully
(`status: done`, no errors).

Ground truth for these calls, and the accuracy/F1/confusion-matrix
comparison against it, is in [VALIDATION.md](../VALIDATION.md) — this
directory is the raw predictions only, matching the brief's required
output schema (§2).
